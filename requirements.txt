# Core pipeline
pyspark>=3.4.0
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
pyarrow>=10.0.0

# Optional extension (Objective 7 lexicon sentiment)
vaderSentiment>=3.3.2

# Only needed to run the pipeline locally via run_all.py
nbformat>=5.7.0
nbclient>=0.7.0
ipykernel>=6.20.0
