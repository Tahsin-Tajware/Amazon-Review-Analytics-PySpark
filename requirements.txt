# Core
pyspark>=3.4,<4.0
pandas>=1.5
numpy>=1.23
matplotlib>=3.6

# Statistical inference for the moderation analysis.
# Spark MLlib does not provide trustworthy standard errors under
# regularisation, and a moderation claim requires them.
statsmodels>=0.14

# Dataset download (Colab / Kaggle convenience)
kagglehub>=0.2

# Java 11 or 17 is required by Spark and is pre-installed on
# Colab and Kaggle. Locally:  apt install openjdk-17-jre-headless
